# 更新日志 · Changelog

本文件记录 **dsh-skin-koi-pond（锦鲤池塘）** 的发布变更。格式参考 [Keep a Changelog](https://keepachangelog.com/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### ✨ 变更
- **CPG 体态层重构（行波几何化 + 关节角域收敛，消除大转弯漂移/抖动）**：调研（Lighthill 体波 / Ijspeert CPG 振子链 / ABZU 数学驱动体态 / bend-and-coast）后的架构升级——真实鱼的游动 = 转弯弯曲（低频）与推进行波（高频）**同一套脊柱几何**，而旧版行波是「渲染层位移叠加」、叠在路径追随骨架上，急弯时两套系统打架（抖动），体态滞后目标场（漂移）。`koiSkeleton` 三处改动：
  - **行波几何化（spineTilt）**：中线倾斜场 `tilt(t) = WAVE_TILT_AMP·t²·sin(ph0 − t·WAVE_SPAN)` 直接写进脊柱目标朝向——摆动由身体本身承担，相位沿弧长向后传播、幅值随 beat/panic 调制（公式沿用原渲染层）。`WAVE_TILT_AMP = 0.156 rad` 由原渲染尾端位移 0.32·baseSize 反推（A·k = A·WAVE_SPAN/L），视觉摆幅等价；**渲染层 wave 恒 0**（再叠加会双重摆动），koiRender 零改动
  - **跟随层改相对角域**：状态量 `sk.rel[i]`（关节相对角）向目标差分 `relT = angNorm(r[i]−r[i−1])` 低通收敛，形态层直接钳 `|rel| ≤ caps`。rel 被 cap 钳为小量 → 无 atan2 wrap 跳变；r/tilt 连续 → relT 连续 → 消除「绝对角域 rate>cap 拉锯」与「目标场 wrap 突变」两大抖动源
  - **解析段长重建**：`len[i] = (nodeTs[i+1]−nodeTs[i]) × bodyArc`（恒定）替代实测段长——重采样边界误差（含近零段 atan2(0,0) 假方向）不再进入重建层；近零段方向继承上一段
  - **回归实测**（种子 20260902）：jointdiag max **30.1° → 24.0°**（= 尾端 cap 精确钳制，p99 20.6°）、折角 **0**；fishshape 体长/段距漂移 **0.98× → 1.00×（严格恒定）**、段距 min=p95=2.87；finshape 单弧尾摆翻转 0:58 1:22（更集中）；stateprobe G/H/I 全过（点击 9.10、逃跑 9.05 vs 躲避 3.71）；bench/validate ✓
  - **后续路线**（阶段 2/3）：转弯改为 drive 偏置沿脊柱传播（头部朝向 = 身体弯曲的积分，路径从体态涌现）；ESCAPE 逃跑 = C-start drive 脉冲

### 🐛 修复
- **路径与体态分离（脊柱状态收敛架构）**：此前 `flexSpine` 是「单帧整形」——body 完全是头部轨迹的弧长重采样，体态只是路径历史的从属产物，没有帧间记忆；本条目把脊柱弯曲升级为**持久状态**，实现路径层与体态层的真分离。`koiSkeleton` 删除 `flexSpine`，新增 `updateSpine(k)`：
  - **数据结构**：`sk.spine[i]` = 每段脊柱的持久绝对朝向（出生时初始化为初速方向 = 直线鱼），配 `sk.followRates[i]`（跟随角速率）与 `sk.jointCaps[i]`（关节上限）；模块级 `spineLenBuf`/`spineDirBuf` 复用缓冲避免每帧分配
  - **三层收敛**：①**跟随层（惯性）**——目标场 r[i] = 轨迹段方向，`spine[i] += clamp(angNorm(r[i]−spine[i]) × FOLLOW_GAIN(0.3), ±followRate(t))`：小弯比例跟随几乎无滞后感，急弯有限速率传播（弯曲沿脊柱向后传播，尾梢滞后约 0.5s）；**头段锁定 r[0]**（`followRate(∞)` = 刚性棍，鱼头永远朝路径方向）②**形态层（安全网）**——相邻差分钳制 `|spine[i]−spine[i−1]| ≤ caps[i]`，从前往后钳、被钳量不需残差后推（下一帧继续追目标，多级弹簧语义）③**重建层**——`body[0]` = 目标场头点（路径锚定），`body[i+1] = body[i] + len[i]·dir(spine[i])`（保长重建）
  - **角速率分布**：上腩段 0.10→0.14、下腹/尾柄段 0.14→0.20 rad/帧（尾端 0.20 ≈ 11.5°/帧，180° 弯曲约 16 帧 ≈ 0.53s 传到尾梢，真实锦鲤 C 形逃逸的量级）；与关节 cap 同为「头小尾大」梯度
  - **时序**：body 组件产出目标场 → skeleton 组件第 0 步 `updateSpine(k)` 读它重建覆写 body——组件注册顺序已保证
  - **架构语义**：对差分限幅的算法里，任何「对整形量（而非角度本身）做二次加工」（如累计整形量整体缩放）都会重新放出被限掉的折角——上一条 🐛 的教训在架构层的正面解法：目标场只读不改，限幅只作用于角度本身
  - **回归实测**（种子 20260902）：jointdiag p99 24.5° → **23.3°**、max 29.3° → **30.1°**（±阈值内）、急转帧 >45° 折角 = **0**、尾部承担占比 p50 = 0.4；stateprobe 点击峰值 **9.10**、围堵逃跑 **9.04** vs 躲避 3.72（爆发语义保留、头部锚定未受影响）；fishshape 体长/段距漂移 **0.98×**、拉断帧 0；finshape 含单弧尾摆 D 全过；bench ellipse 976/帧无劣化；validate ✓
  - **未来扩展**：主动体态项（ESCAPE 时 C 形弓背、S 形游动）只需往目标场加项，架构已预留

### 🐛 修复
- **骨骼节点按真实鱼解剖分布（头刚性 + 头稀疏尾密集）**：参考真实鲤鱼骨架（头不可弯 → 上腩大刺粗硬活动小 → 下腹小刺细密灵活），把身体点列从「等弧长 2.88px 均匀分布」改为**三段密度梯度**：
  - **头部刚性段（前 10% 体长，3 节点）**：关节转角上限 **0°**（鱼头没法弯折），段长最大（≈4px）
  - **上腩大刺段（10%~50%，11 节点）**：转角上限 5°→10° 线性爬坡（大刺粗硬、活动幅度小），段长中等（≈3.2px）
  - **下腹/尾柄段（50%~100%，14 节点）**：转角上限 10°→24°（小刺细密、灵活甩尾），段长最小（≈2.9px）
  - 实现：`koiSkeleton` 新增 `nodeT(i,n)`（节点弧长分布）与 `jointCapRad(t)`（按弧长位置的关节上限），`makeSkeleton` 预算 `sk.nodeTs`/`sk.jointCaps`；`resampleBody` 按非均匀弧长目标采样（段长随节点密度递减）；行波相位/体型轮廓/部位绑定全部从「索引 t」改用「弧长 t」（`nearestIndex` 二分定位），避免波峰随密度漂移
  - **架构语义**：相邻区段不共端点（否则两节点 t 重合 → 重叠点 → 0 段长 → atan2 假阳性 180° 折角，曾致 jointdiag E/F 双 FAIL）

### 🐛 修复
- **flexSpine「整形量整体缩放」限幅失效（单关节 41° vs cap 8°）**：`MAX_TOTAL_TURN_RAD` 收紧遍累计的是「整形量 f[i]−r[i]」而非转角，其差分 ≈ 原始轨迹差分 × scale，重建后完全丢失 cap 语义（实测放出 −41° 折角，cap 只有 8°，jointdiag F 折角 77/795、p99 48.3°）。删除该遍——第一遍残差后推本身已保证单关节 |f[i]−f[i−1]| ≤ cap 且总转角 ≤ Σcaps（≈296°）。修复后 jointdiag：p99 48.3° → **24.5°**、max 177.8° → **29.3°**、急转帧 >45° 折角 77 → **0**、尾部承担占比 p50 = 0.4（弯曲平滑铺开、尾部参与）
- **逃跑更快 + 逃跑全程无视指针**：
  - **爆发速度上调（koiFish）**：`ESCAPE_SPEED` 2.2 → **2.6**、`FORCE_ESCAPE` 0.7 → **0.85** —— 逃跑期望速度 maxSpeed×2.6（约巡航 3 倍），且更大的加速度上限让爆发更快到位
  - **逃跑不再看鼠标（koiFish `computeWish` ESCAPE 分支）**：删除「指针仍很近时叠加远离分量」逻辑 —— 逃跑方向只由最近边缘目标决定，指针再近也不改向（逃命时鱼不会回头看）
  - **jointdiag 有效关节段长门槛 1 → 1.5px**：逃跑甩尾（panic=1 时尾端摆幅 ≈0.53×baseSize，与段长 2.88px 同量级）会把相邻渲染点反向压缩成 <1.5px 的亚像素段，造成 t>0.93 极尾端的表观折角（65°，段长 1.16px）—— 那是甩尾伪影不是脊柱折断（flexSpine 保长重建后 body 真折时相邻段恒 ≈2.88px，不受 1.5px 门槛影响）。剔除伪影后 jointdiag max 76.9°→**32.6°**、急转折角 3→**0**
  - 回归实测（种子 20260902）：stateprobe 点击峰值 7.70 → **9.10**（阈值 3.76），围堵逃跑峰值 **9.07** vs 躲避 3.37；jointdiag E/F 全过；fishshape 体长/段距漂移仍 1.00×

- **掉头时鱼身卷成「蚂蚁蟥」环 + 头直接原地旋转**：用户截图显示鱼在大幅方向转换时被折成接近 360° 的环形卷曲，且鱼头方向直接瞬转（无甩尾惯性），与真实鱼转弯行为不符。两层根因：
  1. **`clampHeading` 在 vel.sp < `FREE_TURN_SPEED` 时取消转向限制**：vel 在 ± 方向跨过 0 时 `hypot(vel)` 接近 0，方向任意 → vel 单帧反向（180° 瞬转），身体在小弧长（≤5 px = 2 段）内被折断
  2. **`flexSpine` 单关节 cap 偏宽**：头 7° / 尾 26° 累计自由度 ≈ 643°，理论上可让鱼身打 1.7 个圈；实测 jointdiag max = 143.4°、急转帧 19/914 (2%) 出现 >45° 折角
  修复（按物理合理性，从运动学→骨骼→状态机→甩尾动效）：
  - **`clampHeading` 永久钳制转向**：删除 `FREE_TURN_SPEED` 例外，所有状态（巡航 0.28 / 躲避 0.24 / 逃跑 0.28 rad/帧）都受 `turnCap` 限制；vel 大小保留为「上一帧 vel 大小 os」（不抬到 SPEED_FLOOR，也不降至 0），vel 永不反向 —— 180° 掉头稳定为 11 帧平滑弧线（弧长 ≈ 7.7×11 ≈ 85 px ≈ 30 段），body 有充足空间铺开
  - **`flexSpine` 收紧 cap + 总净转角上限**：JOINT_HEAD_DEG 7→5 / JOINT_TAIL_DEG 26→12（平均 8.5°）；新增 `MAX_TOTAL_TURN_RAD = 270°`（4.71 rad），当按 r 直接累加估算的任意位置累计转角超过此值时整体均匀收紧 cap —— 杜绝鱼身自卷成圈，永远保留至少 90° 的「直」余量
  - **ESCAPE 入场保留前冲惯性（koiFish `computeWish`）**：fs.t < 9 帧时 wish = 50% 当前 vel 方向 + 50% 朝边缘方向，前冲权重随 fs.t 线性衰减。真实鱼逃跑「离弦而出」先弹射再转向，旧版立即让 vel 朝边缘 → vel 在小弧长内 180° 掉头 = body 在头部附近打成蚂蚁蟥环
  - **甩尾推力强化（koiSkeleton `updateSkeleton` + koiFish `updateMotion`）**：`WAVE_AMP` panic 系数 0.2 → 0.4（panic=1 时尾端摆幅放大 1.4×）；`swimPhase += WAVE_SPEED × beat × (1 + 0.5 × panic)` —— panic=1 时相位推进速度 1.5×（≈ 3Hz 高频甩尾 @ 60fps），让「掉头」伴随真实的快速大幅甩尾，而非头部直接旋转

- **掉头时身体「折断」90°+、尾巴僵直**：身体点是头部轨迹的弧长重采样，掉头/急停时速度方向瞬时翻转 → 轨迹在极短弧长（亚像素段）内折返，整段 180° 转角被压进 1~2 个关节。黑盒基线实测：单关节转角 p99 176°、max 180°；急转帧（净弯 >60°）中 79% 含 >45° 折角，且折角集中在头部（t≈0.3），后 40% 关节只承担约 40% 弯曲（尾巴不参与）。两层修复，令弯曲「头小尾大」平滑铺开、任何单关节不出现折断式转角：
  - **脊柱关节转角限幅 + 残差后推（koiSkeleton `flexSpine`）**：给每个关节按「头小尾大」斜坡设上限（`JOINT_HEAD_DEG 7° → JOINT_TAIL_DEG 26°`，实测任何单关节 ≤~33°），超限残差沿身体向尾部传递 —— 急转的弯曲平滑铺开后半身、尾端甩动承担多出的弯角。头点不动、段长保持原值 → 总长与净转角守恒，只重分布不增删。正常巡游/缓弯所有转角低于上限 → 几何与原实现逐位一致（零回归）
  - **转向角速度上限 + 速度下限（koiFish `clampHeading`）**：运动学层约束掉头为有限半径的平滑弧线而非原地折返（`MAX_TURN 0.28 rad/帧 ≈ 16°/帧`，180° 掉头约 11 帧；`SPEED_FLOOR 0.6 px/帧` 永不停零；旧速低于 `FREE_TURN_SPEED 0.4` 视为静止起步、不设转向限制）—— 消除「轨迹近零位移打结」这一折角的根因
  - 同口径复测：单关节转角 p99 176°→**28.6°**、max 180°→**33°**；急转帧含 >45° 折角 **79%→0**；折角位置从头侧（t≈0.3）移向尾端（t≈0.9），尾巴甩动参与转弯

### 🧪 回归断言
- **jointdiag（脊柱关节角断言，新增 `scripts/jointdiag.mjs`）**：黑盒反解主画布身体点列（按 globalAlpha 单调方向切渐变 run、取递减段 = 主色身体层，与 fishshape 同思路），逐关节测方向差（段长 <1px 的亚像素段剔除，避免测量伪影）。断言 E「有效关节单关节转角 p99 ≤50°」、F「急转帧含 >45° 折角 = 0」，指针以「远距缓追 + 逼近时冲向鱼前方 overshoot」逼迫 180° 掉头。`node scripts/jointdiag.mjs lib/client.js jointdiag`，退出码即断言结果
- **stateprobe 坐标反解修复（状态机 G/H/I 端到端验证打通）**：探针 mock 误把 canvas `ellipse` 推进**全局共享、帧内不清空的 buff**、只在 `fill()` 时用当时 CTM 整批结算 —— 而真实 canvas 语义是每个 context 独立 path、`beginPath()` 清空、fill 只结算本 path（`ellLit` 每次都 beginPath+单椭圆+fill）。这使每条鱼解析出的首点 =「全帧第一个椭圆（阴影层首段）+ 当前 translate」，**整条鱼迹被平移一个常量（实测 +311,+146）**：所有速度/点击/围堵测量都基于错误的幽灵鱼头，点击与真实鱼错位 ~311px，状态机从未被真正触发，G/H 断言恒 FAIL（峰值 4.86 vs 需 ≥5.55）。按真实路径语义修正 mock 后，解析鱼头与 `flock[i].pos` 逐位对齐（偏移 Δ≈0）；同步把「越界环绕的瞬移帧（>400px/帧）」从速度峰值测量中剔除，避免 H1/H2 被瞬移伪影廉价放行。修复后黑盒实测（种子 20260902）：巡航中位 2.51 px/帧；点击惊吓峰值 **7.70**（27 帧，阈值 3.76）并冲边环绕后回落；围堵 2s 后躲避期 3.14 → 逃跑期 **7.70**（爆发 ≥1.08× 纯躲避）、触达边缘环绕 —— G/H/I 全部 ✓

### 🧩 工程重构
- **运行时模块化拆分**：`plugin/koi/koiPond.js` 从单文件约 900 行瘦身为 317 行的主入口（仅保留状态 + 主循环 + 生命周期），渲染 / 骨骼 / 荷叶 / 涟漪 / 鱼 AI 按依赖拆分为 9 个片段（`koiSchemes → koiMath → koiLight → koiSkeleton → koiLeaf → koiRipple → koiRender → koiFish → koiPond`），由 `build.mjs` strip import/export 后按序拼进同一 factory 作用域。**注意**：共享作用域片段不能用 IIFE 包裹，否则状态被关进闭包、其他片段无法访问
- **轻量鱼骨骼系统（koiSkeleton.js）**：新增 `BIND` 部位绑定表（`head 0.08 / pec 0.2 / dorsal0 0.36 / dorsal1 0.6 / tail 1.0`，改部位位置只改这张表）+ `updateSkeleton(sk, k, frameCount)` 每帧输出切线/法线（`sk.tan[]`）、行波 undulation（`sk.wave[]`，头稳尾摆）、部位世界坐标（`sk.bind[name]` 含行波偏移）、鳍动效相位（`sk.flap` 胸鳍扇动 / `sk.spread` 尾鳍张合，随 panic 加大）。渲染层只读骨骼状态 —— 改动效改 `WAVE_SPAN / WAVE_SPEED / WAVE_AMP` 三个常量，不再深挖绘制函数

### 🐛 修复
- **鱼鳍实际不可见（真正根因）**：`drawPectoralTail` / `drawDorsal` 用 `rgba(k.color2, 0.5 * curAlpha)` 传透明度，而 `rgba` 的 alpha 参数是 0-255 语义 —— `0.5` 被当作 `0.5/255 ≈ 0.2%`，胸鳍 / 尾鳍 / 背鳍全部近乎透明。这正是早前「DSH 中看不到鱼鳍」排查中被误判为浏览器缓存/鳍尺寸小的真正原因。修复：鳍的透明度统一补乘 `255`（`0.5*255` / `0.55*255` / `0.42*255`），与 `drawBodyLight` 既有写法一致

### ✨ 改进
- **移除鱼身离散高光**：顶光带与头部高光由一排离散亮椭圆组成，体型小时相邻圆心距超过直径、亮斑逐个显形（串珠感，与早前鱼身串珠同源）。按反馈移除两处高光，仅保留腹侧环境反光（暗部，alpha 0.05，不串珠）与 AO 渐变 —— 立体感保留，但鱼身干净无颗粒
- **实时光照更新**：光源从「固定世界上方」升级为「绕池缓慢移动的实时光源」（Lissajous 轨迹，约 77s 一周）+ 光强低频呼吸（0.76~1.0）。每条鱼每段按「段 → 光源」方向实时计算受光面：`ellLit` 的渐变中心、顶光带、头部高光、腹侧环境反光全部实时跟随光源方向；光方向离散为 8 向，渐变缓存 key 加入方向索引，仍每帧近零新建（实测 gradient 创建 9.4 → 28.9/帧，仅为光源换向时少量新组合）
- **鱼身二维光照强化**：在 AO 渐变（上一轮）基础上补「光照细节层」`drawBodyLight`——①背脊顶光带：受光侧（-y）一排亮椭圆（暖白 `#fffbe8`，alpha 0.11 头部 → 0.04 尾部衰减）；②头顶球面高光：吻部后上方小亮斑（白 alpha 0.16，球面受光最亮点）；③腹侧环境反光：背光侧（+y）深青色带（`#0e3a4a` alpha 0.05，模拟水下环境光而非纯黑阴影）。同时主色 AO 压暗加深（dark 0.72→0.62 头、0.55→0.48 尾），明暗对比更强。绘制顺序：尾腹色层 → 胸鳍/尾鳍 → 主色层(AO) → 光照细节层 → 背脊线
- **鱼身方向光照（AO）**：主色层与尾腹色层从纯色平涂改为径向渐变（`ellLit`）——光从画面上方照下（与背鳍同侧、与投影方向相反）：渐变中心向受光侧偏移，中心=原色（受光）、边缘=原色按 `dark` 倍率压暗。主色层头部亮、尾部整体变暗（边缘压暗 28% → 45%），多段串接后受光侧连成亮脊、背光侧累积成暗边，鱼身呈现「上亮下暗」的立体感。渐变对象按 `(hex, 半径, dark)` 缓存复用、经 `translate` 进入局部坐标系，每帧零创建（实测 gradient 创建 3410/帧 → 9.4/帧）
- **鱼鳍改骨骼驱动的 SVG 贴图（胸鳍 / 尾鳍）**：从矢量椭圆/三角手绘改为独立素材 `plugin/koi/assets/fin-pectoral.svg`、`fin-tail.svg` —— 想换鳍形直接替换素材文件即可（素材内 `#888888` 占位符运行时被逐像素染成尾腹色 `color2` 的剪影，透明度 `globalAlpha` 0.5/0.55 半透明）。单文件产物不能外链图片，`build.mjs` 打包时把 SVG 文本内联进 `__FIN_PEC_SVG__` / `__FIN_TAIL_SVG__` 占位符。贴图方向与相位由骨骼锚点驱动、不再手绘角度：胸鳍从鳃盖后 `bind.pec` 两侧对称贴、随 `sk.flap` 扇动；尾鳍贴图根对准尾端、旋转 = 尾端切线角 +π，任何体型都恒指向鱼尾（「鳍画反」问题根治）。鳍贴图按 `(kind, color2)` 缓存，换配色懒烘新剪影
- **背鳍移除，改画「背脊亮线」**：连续背鳍膜在鱼急转时随身体法线翻转而整体撕向另一侧，转弯完全没有整体感。删除 `drawDorsal`，改为沿背部中线（`bind.dorsal0 → bind.dorsal1`，t≈0.36–0.6）描一条比主色更亮的圆头细线（`lighten(k.color, 0.4)`，线宽 `baseSize × 0.1`）—— 中线随身体整体弯曲、永不翻转，转弯与鱼身浑然一体，只留「背上有条亮脊」的意象
- **尾摆行波相位按体长归一化（`WAVE_SPAN`）**：相位从「弧度/段 × 段号」改为「归一化位置 × WAVE_SPAN(2.8，≈ 半波长)」——身体段数 ∝ 体长，原式下大鱼身上会挤进 2+ 个完整正弦（S 形蛇游）、小鱼却不足一个波，观感随体型漂移。归一化后任何体长的鱼都是平滑单弧尾摆；`WAVE_SPEED 0.2→0.22`（≈ 0.48s/拍 @60fps、巡航 ~2Hz）、`WAVE_AMP 0.24→0.32`，尾部摆幅更舒展。由 finshape 断言 D「单弧尾摆（翻转 ≤1）」防回归
- **移除鱼头眼睛**：按用户要求删除 `drawEyes`（黑眼仁 + 白高光）及其调用 —— 实测 `arc`/帧从 54 降到 8.9（仅剩涟漪）
- **鱼有大有小了**：新增独立体型参数 `sizeVar = rnd(0.72, 1.35)`，`baseSize` 改为 `max(10, round(rnd(12,20) × sizeVar))`（原为 `floor(rnd(15,20))` 只有 5 档）。体宽与体长都由它直接驱动 —— 同池里最小鱼吻部直径 ≈ 8.6px、最大 ≈ 17.1px（2.0×），体长约 57px ~ 161px
- **告别蝌蚪形，轮廓改为锦鲤俯视比例**：原 `bodySize` 是「头部 1/6 处即最宽、随后线性缩到 0」的蝌蚪形；现在用归一化轮廓关键点 + smoothstep 插值（`BODY_PROFILE`）：吻部圆钝 0.75 → 头冠 0.95 → 鳃盖后最宽 1.15（体长 1/4 处）→ 中段 1.0 → 尾柄 0.45 → 尾柄末 0.28（不尖）。实测尾柄/鳃盖 ≈ 0.39，接近真实锦鲤（0.30-0.35）
- **头部更精致**：头部从只占体长 1/6 延长到前 1/4 且平滑过渡（眼睛已移除），不再是「一个圆点 + 突然变宽」
- **身色渐变改为显式曲线**：主色 alpha 头 0.9 → 尾 0.3，尾腹色 alpha 头 0.35 → 尾 0.75 —— 头部实、尾部透出尾色，且不再依赖 `bodyLength` 数值（原来随体型漂移）
- **轨迹容量随体型增大**：`TRAIL_MAX` 192 → 288，覆盖最大体型（体长 167px）在 0.58px/帧 极端低速下的轨迹需求

### 🐛 修复
- **锦鲤躲闪时被拉长、并露出间隔纹路**：身体点原先直接取「每帧头部位置」，体长 = 段数 × 每帧位移，躲闪时速度涨到约 2 倍，实测体长从 82px 拉到最差帧 177px；同时相邻椭圆的圆心间距从 2.8px 涨到 4.7px、重叠层数掉约四成，圆心逐个显形 —— 就是肉眼看到的「间隔纹路」。现在头部位置先写入按帧记录的轨迹，再按**固定弧长**重采样出身体点列，体长与段间距都与瞬时速度无关（实测段距恒定 2.87px，漂移 1.80× → 1.00×）
- **环绕瞬移把鱼身拉断**：原实现只在 `edges()` 里平移轨迹，而身体点存的是绝对坐标，鱼游出屏幕的瞬间身体会被拉成横跨全屏的一条线（实测任意时刻约 2~3 条鱼处于撕裂状态）。改为身体由轨迹重采样后，轨迹整体平移、身体自然跟随，撕裂帧归零

- **阴影不再随亮暗主题切换**：鱼影与荷叶影原先恒为近黑 `rgba(8,8,8,…)`，而荷叶面会跟随主题变色，导致「宣纸日色」下阴影留下一片脏灰。现在两者都跟随 `data-ds-dark-theme`（暗 `rgba(2,8,14,…)` / 亮 `rgba(30,46,38,…)`）
- **鱼影偏移与体型脱钩**：原实现硬编码 `+50,+50` 偏移，对直径仅 30~40px 的锦鲤来说影子会整个脱开本体、看着像另一条鱼。改为跟随体型 `baseSize × 1.15`，荷叶影则沿用原先的按 `size` 缩放

### ✨ 改进
- **体长系数对齐原巡航观感**：`BODY_ARC_RATIO` 由 7 调整为 5.75，让恒定段距（2.87px）与原巡航时的实测点距（2.84px）一致 —— 保留原有观感，只去掉随速度漂移的那部分
- **阴影柔化**：影子先画在 0.35 倍分辨率的离屏层上，再放大贴回主画布 —— 借浏览器的双线性插值得到柔和投影，取代原先一堆硬边椭圆；顺带把阴影的填充开销压到约 1/8
- **移除外层模糊**：`#koi-pond-scrim` 不再使用全屏 `backdrop-filter: blur(2px)`。它盖在 canvas 之上会把锦鲤与荷叶细节一起糊掉，且 canvas 每帧都在变、backdrop 每帧都要重算。改为沉到 canvas 下层（`z-index` 0 / canvas 1）的渐变雾面，只作用于 body 的池水渐变

### ⚡ 性能
- **弧长重采样几乎零成本**：身体点重采样每帧新增约 0.066ms（12~15 条鱼，约占 30fps 帧预算 33ms 的 0.2%）；轨迹点对象循环复用、每帧零分配；绘制调用数不变（椭圆填充 1350/帧 → 1354/帧）
- **荷叶离屏烘焙**：荷叶是静态的，原实现每帧要为 8 片荷叶跑约 8,800 次 `lineTo` 与同量级的 Perlin 噪声（外加 192 条叶脉求半径）。现在在 mount / 主题切换 / dpr 变化时把每片荷叶烘焙成两张位图（阴影与叶面分开，以保留「荷叶影在鱼下、荷叶面在鱼上」的遮挡层次），运行时每帧只剩 16 次 `drawImage`，噪声与折线计算归零

### 🧪 回归断言
- **finshape（鳍几何断言，新增 `scripts/finshape.mjs`）**：黑盒驱动 `lib/client.js` 真实产物，从主画布绘制流反解贴图 CTM 与 stroke 折线，断言 A) 胸鳍贴图朝「尾向偏外」、B) 尾鳍贴图朝尾向伸展、C) 背脊线 = 沿身体中线的 stroke（存在且连续，旧整膜锯齿必 FAIL）、D) 行波单弧尾摆（横向翻转 ≤1，多波蛇游必 FAIL）。与 `fishshape`（体形）/ `bench`（绘制调用）并列：`node scripts/finshape.mjs lib/client.js finshape`

## [0.1.0] - 2026-08-28

首个公开版本。

### ✨ 新增特性
- **锦鲤池塘主题**：墨青池水为底、水波涟漪为纹、锦鲤橙红点睛，含「宣纸日色」(亮) 与「池塘夜色」(暗) 双主题
- **客户端锦鲤画布动画**：轻量、纯展示层，不占用额外服务
- **按 7 部件拆分 CSS**：`background / sidebar / titlebar / composer / overlay / fonts / ui`，遵循 `skin.json` 主题规范
- **9 套锦鲤配色预设 + 随机彩蛋**：支持 `window.__koiSetScheme(id)` 在控制台即时切换
- **中英双语文档**与**社区健康文件**（`CONTRIBUTING` / `CODE_OF_CONDUCT` / `SECURITY`、issue/PR 模板）

### 🧹 变更
- 主题改为**自描述**（standalone DSH 皮肤），移除对外部 `dsh-theme-manager` / `theme-manager` /「托管模式」的引用与依赖
- README 顶部补充 `version` / `stars` / `last-commit` 徽章

### 📦 安装
```bash
dsh plugin --profile web add github:Kogisune/dsh-skin-koi-pond
# 重启 dsh web 生效；卸载即复原
```

### 🎨 自定义配色
打开浏览器控制台，切换任意预设：
```js
window.__koiSetScheme('kohaku')   // 红白
window.__koiSetScheme('random')   // 随机彩蛋
```

### ⚠️ 范围说明
本主题是 DSH WebUI 的**纯展示层**，仅覆盖 DSH 官方设计令牌（`--dsw-alias-*`）与部件样式；不引入服务、不发送 Cordis 事件、不触达模型请求。

### 📸 预览
见 `preview/light.png`（亮色）与 `preview/dark.png`（暗色）。建议在 GitHub Release 中作为附件上传，增强分享卡片展示。
