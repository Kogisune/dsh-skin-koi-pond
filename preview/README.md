# 预览图（Preview）

已就绪 ✅（1920×1080 PNG）：

- `preview/light.png` — 浅色主题（宣纸日色）
- `preview/dark.png` — 深色主题（池塘夜色）

`skin.json` 已启用：

```jsonc
"preview": { "light": "preview/light.png", "dark": "preview/dark.png" }
```

重新截图后直接覆盖同名文件即可（GitHub README 与 dsh-theme-manager 面板自动读取）。
